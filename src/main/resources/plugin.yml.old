name: ${name}
main: ${main}

author: GalaxyVN

api-version: 1.13
version: ${version}

lib-version: ${libVersion}
lib-download: ${libDownload}
loader-version: ${loaderVersion}

github: https://github.com/Galaxy-VN/${name}
description: Advanced Control Player Thought A Gui

softdepend:
  - SkinsRestorer

built-time: ${built}
built-by: ${builder}
built-os: ${os}